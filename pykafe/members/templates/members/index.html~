<!DOCTYPE html>
<html>
    <head>
        <link type="text/css" rel="stylesheet" media="screen" href="{{STATIC_URL}}css/bootstrap.min.css"/>
        <link type="text/css" rel="stylesheet" media="screen" href="{{STATIC_URL}}css/bio.css"/>
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>
             Edit
        </title>
      </head>
      <body>
        <nav class="navbar navbar-default">
           <div class="container-fluid">
            </div>
        </nav>
        
        <p class="welcome">Welcome {{ user.get_full_name }}</p>

        <div class="container">
            <div class="row">
                <div class="col-md-6">
                    <form class="form-horizantal" action="." method="post" enctype="multipart/form-data">
                        {% csrf_token %}
                        
                        <div class="col-sm-6">
                            {{ form.first_name.label_tag }}
                            {{ form.first_name }}
                            {{ form.first_name.errors }}
                        </div>
                       
                        <div class="col-sm-6">
                            {{ form.last_name.label_tag }}
                            {{ form.last_name }}
                            {{ form.last_name.errors }}
                        </div>
                        
                        <div class="col-sm-6">
                            {{ form.jobtitle.label_tag }}
                            {{ form.jobtitle }}
                            {{ form.jobtitle.errors }}
                        </div>
                        
                        <div class="col-sm-12">
                            {{ form.biotext.label_tag }}
                            {{ form.biotext }}
                            {{ form.biotext.errors }}
                        </div>
                        
                        <div class="col-sm-12">
                            {{ form.photo.label_tag }}
                            {{ form.photo }}
                            {{ form.photo.errors }}
                        </div>

                        {{ form.non_field_errors }}
                        
                        <button id="submit" class="btn btn-primary">Submit</button>
                    </form>
                </div>
                <div class="col-md-6">
                 {% if object.photo %}
                    <img src="{{ object.photo.url}}" class="img-circle circle-border" width=200 height=200/>
                  {% endif %} 
                    <p class="person-name">{{ user.get_full_name }}</p>
                    <p class="person-position">{{ user.jobtitle }}</p>
                    <p class="person-about">{{ user.biotext }}</p>
                </div>
            </div>
        </div>
    </body>
</html>
		
